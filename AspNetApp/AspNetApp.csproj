<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <!-- Optional properties -->
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Add any necessary NuGet package references here -->
    <!-- This example adds Swagger for API documentation -->
    <PackageReference Include="Swashbuckle.AspNetCore" Version="6.6.2" />

    <!-- If you reference another local project (e.g., a data library) -->
    <!-- <ProjectReference Include="../MyDataLibrary/MyDataLibrary.csproj" /> -->
  </ItemGroup>

</Project>